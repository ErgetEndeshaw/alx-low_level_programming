#include <stdio.h>
/**
 * main - write a program that prints alphabets in lowercase
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	char C;

	C = 'a';
	while
		(C <= 'z') {
			putchar(C);
			C++;
		}
	putchar('\n');
	return (0);
}
