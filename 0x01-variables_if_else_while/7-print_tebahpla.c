#include <stdio.h>
/**
 * main - Entry point
 * Write a program that prints the lowercase alphabet in reverse
 * Return: Always return 0 (success)
 */

int main(void)
{
	char c;

	c 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}

