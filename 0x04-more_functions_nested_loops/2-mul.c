#include "main.h"
/**
 *_mul - Multiplies two integers
 * @:a- The first number to multiply
 * @:b- The second  number to be multiply with the first number
 * Return: The result of multiplication between a param and b param
 */

int _mul(int a, int b)
{
	return (a * b);
}
